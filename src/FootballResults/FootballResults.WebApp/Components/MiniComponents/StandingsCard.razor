@if (Standings != null)
{
    <div class="contentGroup">
        @if (IsGroupStage())
        {
            @foreach(var standingGroup in GetStandingGroups())
            {
                <h2>@standingGroup.group</h2>
                <div class="table-responsive">
                <table class="table table-striped table-borderless standingsTable">
                    <thead>
                        <tr>
                            <th scope="col">#</th>
                            <th scope="col">Team</th>
                            <th scope="col">P</th>
                            <th scope="col">W</th>
                            <th scope="col">D</th>
                            <th scope="col">L</th>
                            <th scope="col">GD</th>
                            <th scope="col">Pts</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (Standing standing in standingGroup.standings)
                        {
                            <tr class="standing">
                                <td>@standing.Rank</td>
                                <td>
                                    <a class="teamDiv" href="teams/@standing.Team.Name">
                                        <div class="teamLogoDiv">
                                            <img class="teamLogo" src="@standing.Team.LogoLink" alt="@standing.Team.Name logo" />
                                        </div>
                                        <div>@standing.Team.Name</div>
                                    </a>
                                </td>
                                <td>@standing.Played</td>
                                <td>@standing.Wins</td>
                                <td>@standing.Draws</td>
                                <td>@standing.Losses</td>
                                <td>@standing.Scored:@standing.Conceded</td>
                                <td>@standing.Points</td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
            }
        }
        else
        {
            <div class="table-responsive">
                <table class="table table-striped table-borderless standingsTable">
                    <thead>
                        <tr>
                            <th scope="col">#</th>
                            <th scope="col">Team</th>
                            <th scope="col">P</th>
                            <th scope="col">W</th>
                            <th scope="col">D</th>
                            <th scope="col">L</th>
                            <th scope="col">GD</th>
                            <th scope="col">Pts</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (Standing standing in Standings)
                        {
                            <tr class="standing">
                                <td>@standing.Rank</td>
                                <td>
                                    <a class="teamDiv" href="teams/@standing.Team.Name">
                                        <div class="teamLogoDiv">
                                            <img class="teamLogo" src="@standing.Team.LogoLink" alt="@standing.Team.Name logo" />
                                        </div>
                                        <div>@standing.Team.Name</div>
                                    </a
                                </td>
                                <td>@standing.Played</td>
                                <td>@standing.Wins</td>
                                <td>@standing.Draws</td>
                                <td>@standing.Losses</td>
                                <td>@standing.Scored:@standing.Conceded</td>
                                <td>@standing.Points</td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        }

        <p>Last update @GetLastUpdate()</p>
    </div>
}
else
{
    <LoadingSpinner />
}

@code {
    [Parameter]
    public IEnumerable<Standing>? Standings { get; set; }

    private bool IsGroupStage()
    {
        return Standings!.Select(s => s.Group).Distinct().Count() > 1;
    }

    private List<(string group, List<Standing> standings)> GetStandingGroups()
    {
        return Standings!
        .GroupBy(
            s => s.Group,
            (group, standings) => (group, standings.OrderBy(s => s.Rank).ToList())
        )
        .ToList();
    }

    private DateTime? GetLastUpdate()
    {
        return Standings!
            .Select(s => s.LastUpdate)
            .Max();
    }
}
